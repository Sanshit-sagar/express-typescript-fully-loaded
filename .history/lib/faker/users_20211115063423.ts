import {
    createUser,
    createAddress,
    createHackerDetails
} from './user'


const createUsers = (userCount: number) => {
    if(userCount <= 1) throw new Error('') userCount > 20) {
        throw new E
    }
}
